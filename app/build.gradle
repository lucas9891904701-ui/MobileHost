plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    // Deve vir depois do plugin do Android. Empacota um CPython real
    // (ARM64 nativo) dentro do app para o terminal interno rodar
    // "python3 arquivo.py" sem depender do Termux.
    id("com.chaquo.python")
}

android {
    namespace = "com.mobilehost.app"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.mobilehost.app"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"

        ndk {
            // Só ARM64: é o alvo pedido e evita empacotar o interpretador
            // Python várias vezes para outras arquiteturas (APK menor).
            abiFilters "arm64-v8a"
        }
    }

    buildTypes {
        release {
            minifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.activity:activity-ktx:1.9.1")
    implementation("androidx.lifecycle:lifecycle-livedata-ktx:2.8.4")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")
}
